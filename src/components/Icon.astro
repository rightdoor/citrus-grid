---
interface Props {
  d: string
  class?: string
}

const { d, class: className } = Astro.props
---

<svg class:list={['mdi-icon', className]} width="1em" height="1em" viewBox="0 0 24 24" fill="currentColor" aria-hidden="true">
  <path d={d}></path>
</svg>
