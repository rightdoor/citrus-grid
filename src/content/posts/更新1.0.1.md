---
title: "更新1.0.1"
slug: geng-xin-1-0-1
index: 2
description: "修复 visitor-stats 插件一次大量读取导致统计服务429报错问题以及统一路由尾斜杠格式。"
category: "版本更新"
tags: [更新说明]
published: 2026-09-03 19:54:11
---

## 1.0.1 更新

### 修复 visitor-stats 插件一次大量读取导致统计服务429报错问题

1、修复 visitor-stats 插件的同时对项目 [visitor-stats](https://github.com/rightdoor/visitor-stats) 进行了一次更新，使用 `/total` 来一次获取全部数据，使用 localStorage 来缓存数据。

流程：

- 访问 `/total` 接口，获取全部数据
- 缓存数据到 localStorage 中
- 从 localStorage 中读取数据，避免重复请求（TTL 为 5 分钟，过期后重新请求）。

2、修复了其他一些小问题，如路由缓存策略、URL 规范等。

---

### 统一路由尾斜杠格式，前端路由的缓存策略与托管平台（Cloudflare Pages）的 URL 规范严格对齐

在进行页面切换时发现了多次重复请求预取的问题，导致页面流量异常，经排查为预取请求与 Canonical URL 不一致造成的，现在已修复。
